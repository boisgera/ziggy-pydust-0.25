[build-system]
requires = ["poetry-core", "ziggy-pydust==0.25.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "ziggy-pydust-template"
version = "0.1.0"
description = ""
authors = ["Fulcrum Inc. <hello@fulcrum.so>"]
readme = "README.md"
packages = [{ include = "sandbox" }]
include = [
    { path = "src/", format = "sdist" },
    { path = "sandbox/*.so", format = "wheel" },
]

[tool.poetry.build]
script = "build.py"

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
ziggy-pydust = "0.25"
pytest = "^8.0.0"
ruff = "^0.4.0"
black = "^23.7.0"

[tool.black]
line-length = 120

[tool.ruff]
line-length = 120
select = ["F", "E", "W", "UP", "I001", "I002"]

[[tool.pydust.ext_module]]
name = "sandbox._lib"
root = "src/sandbox.zig"
